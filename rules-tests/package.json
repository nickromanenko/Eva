{
  "name": "rules-tests",
  "private": true,
  "scripts": {
    "verify": "bash ../scripts/verify-rules.sh"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.0.0"
  }
}
