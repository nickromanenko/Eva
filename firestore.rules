rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Deny all access until real rules are written
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
