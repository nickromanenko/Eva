# Canvas change list — 2026-08-30 decisions

A work order for a `DesignSync` session against project `ed46e806-ffe6-43c7-9660-01c2cb4b625c`
("Eva App Design Prototype"). Each row: file · where · **current** → **new** · why. Apply,
re-pull the mirrors into this folder, then delete this file.

Rail keys and constants are the names inside each `.dc.html`'s inline script. "Placeholder,
pending #n" means the decision fixed *that* something changes but not *to what*; draw a
visibly provisional value, never a plausible one.

Decisions (A-numbers) are in `../reviews/2026-08-30-prd-and-design-review.md` §8;
launch items in `../LAUNCH.md`.

---

## 1. Navigation (A4, #25 Q3)

| File | Where | Current | New | Why |
|---|---|---|---|---|
| Eva Design System | "Navigation & FAB" `tabs` | Home · Calendar · Profile | Home · Calendar · Eva Chat · Learn · Profile | A4 |
| Eva App | `tabs` | as above, five | unchanged — this is the reference | A4 |
| Eva Nutrition Coach | `tabs` (Today · Log · Scan · Chat · Settings) | own five-tab bar | **remove the bar.** The coach is a pushed flow from the Dashboard "Scan meal / Set up meals" shortcut. Today keeps its header; Log, Scan and Settings become header actions or in-page links ("Full meal log", "Add a meal", a gear icon); Chat routes to the Eva Chat tab with nutrition context | #25 Q3, A5 |
| Eva App | `tabs` Eva Chat / Learn `onClick` toasts "out of scope" | toast | leave until §7 draws them; change the toast text to "Not yet drawn" so it is not read as a scope decision (A1 made everything v1) | A1 |

## 2. Copy the architecture cannot honour (A10, review E1–E5)

| File | Where | Current | New | Why |
|---|---|---|---|---|
| Eva App | `privacy` footer | "Health entries are encrypted on your device before they sync. Eva never sells data and never shares it with advertisers." | "Your health entries are encrypted in transit and at rest, never sold, never shared with advertisers, and deletable in full." | A10 |
| Eva App | `resetDone` body | "You're signed in on this device. Other devices were signed out." | "You're signed in on this device." — restore the second sentence only when #76 ships | E2 |
| Eva App | `signupErr` linking card title + body | "This email already uses Apple sign-in · We won't create a second profile. Continue with Apple and everything you've logged stays in one place." + CTA "Continue with Apple" | "This email already has an Eva account · Log in with the sign-in you used before, and everything you've logged stays in one place." CTA "Log in" → `login`. The API cannot name the provider; see #77 | E4, #7 |
| Eva App | `danger` modal body | "…within 30 days…" and the export card | already corrected in code (DESIGN.md §9a); align the artboard: "…straight away…", keep the export card and "Export data instead" **only if #58 ships first** | #8, #58 |
| Eva App | `settings` danger card | "Removes all logs, notes and predictions." | "Removes your cycle history, logs and notes." | #8 |
| Eva App | `day` spec note 2 | "…an Undo toast stays for 5s, the record is purged after." | "…an Undo toast stays for 5s; the entry stays recoverable for 30 days (Undo restores it), then is purged." | C2, #28 |
| Eva App | `home_off`, DS "Couldn't sync" card; Nutrition `scOff` | offline states | **keep** — A3 makes offline v1 | A3 |
| Eva App | `notifications` preview line | "Eva has an update" | **keep** — A9 | A9 |

## 3. Calendar (A6, A7, C4, C7–C9, #23, #24)

| File | Where | Current | New | Why |
|---|---|---|---|---|
| Eva App | `modes` segmented control (Cycle · Planning · Pregnancy · Postpartum) on `cal` | four-way switch | **remove.** Replace with a read-only mode chip ("Cycle" / "Pregnancy · week 14") that is not a control. Add the one-time dismissible prompt card "Trying to conceive or pregnant? Pregnancy Mode changes what Eva tracks." → `pregMode`, with an × that never returns | A6, PRD §Turning it on |
| Eva App | `eventTypes` (5 items) | Sex `onClick` logs immediately with a toast | Sex opens a new sheet `sex`: "Logging to <date>", one large **Log** button, collapsed "Add details" revealing Protection (protected / unprotected / other) and Type (solo / partnered), both optional; toast + Undo on save. Neutral icon, no imagery | A7 |
| Eva App | `eventTypes` | five types | add **Positive test** (available in Cycle and Planning) — one step, "Mark today as a positive test", neutral language, no celebration; it does not change the mode (PRD §Positive test and confirmation) | C4 |
| Eva App | `monthChips` | twelve months of one year | add a year row (‹ 2026 ›) above the month chips | C7, PRD §View 2 |
| Eva App | `dayEntries` `onEdit` | no branch for cycle entries | Edit on a "Menstrual cycle" row opens `cycle` pre-filled; add a row-level action **"Mark period end"** on the last flow day (PRD §Menstrual cycle) | C8, #75 |
| Eva App | `bodyChips` / `BODY_CHIPS` | Bloating, Cramps, Headache, Nausea, Breast tenderness, Back pain, Acne, Cravings, Anxious, Stressed, Brain fog, Appetite changes | Bloating, Cramps, Headache, Nausea, Breast tenderness, Back pain, Acne / breakout, Cravings, Anxious, Stressed, Brain fog, Poor appetite, Heavy appetite, **Libido** (opens a low / high picker on tap) | #24, `api/scripts/seed-refdata.ts` |
| Eva App | `EXTRA_CHIPS` ("More…") | Dizziness, Insomnia, Hot flushes, Low libido, Constipation, Spotting | Dizziness, Hot flashes, Constipation, Loose stool, Insomnia, **Discharge** (opens dry / sticky / creamy / watery / egg-white), Itching | #24, C9 |
| Eva App | `cycleChips` (inline in the flow sheet) | Cramps, Bloating, Back pain, Headache, Low energy, Cravings | Cramps, Bloating, Back pain, Headache, Cravings — drop "Low energy" (energy is the 1–5 scale) | #24 |
| Eva App | `flowOptions` | Spotting · Light · Medium · Heavy as four peers | keep four rows but set Spotting apart: hint "Marker only — doesn't start a period", drawn without the flow swatch scale | #23 |
| Eva App | `legend` | six items | add "Positive test" and, once #75 lands, nothing else | C4 |
| Eva Design System | "Five-point scale" | dot-size cells | emoji + word cells as the App canvas draws (`scales`) — one component, the App one | C6/D2, PRD §Body signals |

## 4. Profile and Settings (A8, A12, A14, A2, #7, #58)

| File | Where | Current | New | Why |
|---|---|---|---|---|
| Eva App | `editProfile` fields | Name · Date of birth · Height · Weight, then three disclosure sections | keep DOB (A8); add an 18+ inline rule under DOB ("Eva is for adults 18 and over"); Height/Weight accept both systems per the Units row | A8, A12, A2 |
| Eva App | `discloseSections` → Goals & lifestyle chips | Steady energy, Better sleep, Understand my cycle, Build strength, Manage stress, Conceive within a year | unchanged — this is the canonical list | A8 |
| Eva App | `discloseSections` → Health information | PCOS, Endometriosis, Thyroid condition, Anaemia, None of these | add **Diabetes, Coeliac disease, Food allergies** | A8, PRD §Nutrition Edge cases 6 |
| Eva App | `discloseSections` → Hormonal medications | Combined pill, Progestogen-only pill, Hormonal IUD, Implant, HRT, None | unchanged — canonical | A8 |
| Eva App | `discloseSections` | (no activity, no sports) | add **Activity** (one of: Mostly sitting, Lightly active, Active, Very active) and **Preferred sports** (Strength, Running, Yoga, Pilates, Cycling, Swimming, Dancing, Walking) | A8 |
| Eva App | `settingsSections` → Personal profile | "Lifestyle — Sleep, work pattern, stress" | "Activity — Mostly sitting … Very active" | A8, D4 |
| Eva App | `settingsSections` → Eva experience | Language "English (UK)" · Units "Metric" | Language "English" · Units "Metric" with meta "Follows your region by default" | A2/A16, A17 pending |
| Eva App | `settingsSections` | (no subscription) | add section **Subscription**: "Manage subscription" (opens App Store management), "Restore purchases" | A14, #88 |
| Eva App | `settingsSections` → Privacy and security | "Manage connected accounts — Apple" | keep; **draw the linking flow**: row → list of providers with Link / Unlink, confirmation "Link Google to this account? You'll be able to log in either way." | #7 |
| Eva App | `privacyRows` → "Data export — JSON, CSV" | row | keep; pending #58 for format | #58 |
| Eva App | `settings` footer | "Eva 2.4.1 · Not a medical device" | keep; version from the build | — |
| Eva Nutrition Coach | `nutRows` Units / Health conditions | duplicated editors | make them **read-only rows that deep-link to Profile** ("Set in Profile ›") | D5 |

## 5. Nutrition (A8, #25, #26)

| File | Where | Current | New | Why |
|---|---|---|---|---|
| Eva Nutrition Coach | `ACTS` (Step 4) | five bands incl. Extremely active | four: Mostly sitting · Lightly active · Active · Very active, descriptions from the App canvas' Activity section; add the line "From your profile — change it there" | #25 Q4, A8 |
| Eva Nutrition Coach | `guard` (Step 5) BMI floor: "Lowest supported target: 59.5 kg" | a number | placeholder "Lowest supported target: — (pending #26)" — no number until #26 | #26 |
| Eva Nutrition Coach | `fitScore` / "Fit 78" / "Nutrition fit" | a named 0–100 | label placeholder **"[score name pending #25]"**, keep the number and breakdown | #25 Q1 |
| Eva Nutrition Coach | `calcRows` and `summaryRows` numbers | 1,455 / 1,760 / 1,804 / +140 | **mark every number "illustrative"** in a caption; they do not reconcile and are not spec (#25 Risks) | #25 |
| Eva Nutrition Coach | `sPreg` "Pregnancy · week 22" block | shows kcal targets in pregnancy | keep drawn, add a spec note "numeric targets in pregnancy ship only if #26 allows" | #26 |

## 6. Auth states not drawn (#6, #7, #38, #61)

New rail items under **Onboarding** in Eva App:

| Rail key (new) | Screen | Spec |
|---|---|---|
| `activated` | "Account activated" — landing after the universal link | one line, one CTA "Go to Eva"; if the app is not installed, the web page says to open the app |
| `notActivated` | Login refused because the address is not activated (told apart **after** the password verifies, #6) | "Check your inbox first" + Resend (60 s throttle, toast) — same layout as `activation` |
| `linkExpired` | Activation or reset link past its window | "This link has expired" + "Send a new one" |
| `rateLimited` | `429 RATE_LIMITED` on sign-up / sign-in | inline banner "Too many attempts. Try again in a minute." — Information tone, not Error; #38 |
| `unreachable` | Launch with a token but no API answer (exists in code, `UnreachableView`) | "Eva can't reach its servers" + Retry + Log out (must be reachable, ARCHITECTURE §5) |
| `googleWeb` | Google sign-in web view | system sheet, no redirect to browser (PRD §Sign Up 4) |

## 7. Screens the Dashboard links to and nothing draws (review §4.1, #10)

| Rail key (new) | Reached from | Spec |
|---|---|---|
| `cycleHistory` | cards D, C, PLAN, H "View cycle details / history / window details / pattern" | current cycle day and phase with the confidence sentence; list of logged cycles with length; "How this is estimated" sheet (A11 — no numbers until #26); for PLAN, the window with its confidence band |
| `contactOptions` | `home_flag` "View contact options" | the country table (LAUNCH §4.3): local emergency number, "contact your maternity provider" wording, neutral fallback |
| `supportResources` | `home_loss` | per-country list, neutral fallback; permanently dismissible |
| `evaChat`, `learn` | tabs | **out of this list** — A5 spec exists for chat; both are their own design phase after Calendar/Dashboard (A13) |

## 8. Screens the launch requires (#86, #88)

| Rail key (new) | Screen | Spec |
|---|---|---|
| `consent` | Between `activation`/first login and the first log | Title "Before you start"; two toggles, **neither pre-selected, no "accept all"**: (1) "Store my health entries so Eva can work" — collect consent; (2) "Let Eva use trusted service providers to process my entries" — share consent, with a "Who?" link listing the processors; a version label; CTA disabled until (1) is on; footer link to the health-data privacy policy. Copy per DESIGN.md §8; wording is *counsel*'s to confirm (#86) |
| `paywall` | After consent, before the first log; and from Settings → Subscription | Trial length and price as placeholders "[trial] · [price]/[period]"; the four App Store 3.1.2 elements: price, duration, auto-renew statement, links to Terms and Privacy; "Restore purchases"; no dark patterns (the close control is a 44 pt target, present from the first frame) |

## 9. Pregnancy journey — to be drawn in its own phase (A13 puts it after Chat)

Listed so it is not forgotten, not to be drawn now: confirmation + dating (LMP / doctor's date / IVF), week counter with day, trimester, EDD range and dating method, appointment-timeline pre-population, folic-acid screen, cycles-trying note, "End pregnancy tracking" in Settings + post-loss support line, delivery details, feeding quick-action (timer + manual), Planning and Postpartum month views, pregnancy and postpartum chip sets, the red-flag escalation card triggered from the Body signals sheet, EPDS only if #26 allows (A20).

## 10. After applying

1. Re-pull all three `.dc.html` files into this folder (`DesignSync get_file`).
2. Update `docs/DESIGN.md` §6 (five-point scale) and §7 (tab bar) to the new canvas, and remove the "pending canvas changes" note.
3. Delete this file.
