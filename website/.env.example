# Copy to website/.env (gitignored). Read at build time and inlined into the
# pages that call the API (/activate, /reset). In CI it comes from the
# API_BASE_URL repo variable — see .github/workflows/deploy-website.yml.
PUBLIC_API_BASE_URL=http://localhost:3003
